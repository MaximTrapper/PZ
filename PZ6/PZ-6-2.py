# Ввод списка A
N = int(input("Введите размер списка A (< 15): "))
A = [int(input(f"Введите элемент {i + 1}: ")) for i in range(N)]

# Создание списка B с элементами на нечетных позициях
B = A[1::2]

# Вывод результата
print(f"Размер списка B: {len(B)}")
print(f"Содержимое списка B: {B}")